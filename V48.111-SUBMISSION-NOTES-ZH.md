# V48.111 投稿前运行与论文对齐说明

## 1. 不建议额外 finetune

V48.111 CNRO 是 audit-only 的闭式严格凸 ridge 实验，不存在 epoch/LR 继续训练问题。三 regime closed-loop 应使用 V48.111 链条实际依赖的冻结 L80 best checkpoint 与其冻结 bucket calibration。为了“再降一点 loss”临时续训会改变 checkpoint、校准和 provenance，从而破坏已有归因链；而此前版本已多次显示 loss 继续下降并不等价于 Support/Reserve 或跨 split transfer 改善。因此投稿前优先完成 frozen-model full evaluation，而不是追加训练轮次。

## 2. 为什么没有把 CNRO 直接插入 runtime planner

V48.111 的 preregistered Support/Reserve transfer gate 尚未观察到结果。把 audit ridge score 直接变成在线 admission/ranking 会构成未经验证的算法 promotion，并使论文无法区分“当前部署算法”与“机制识别实验”。本交付保留 CNRO audit 身份，同时新增完整 submission evaluation harness，使仓库可以端到端跑 Safe/Near/Contact 全指标。

## 3. 论文修改原则

主文保留并收紧：OC-MERO observation-consistent deployability、actuator-realizable recovery、support-reserve complementarity、RIFA、unified Safe/Near/Contact semantics。

V48.111 的核心思想被写成当前 identifiability/representation principle：同一场景不同 candidate 会改变 limiting interaction，因此 invariant object 不是 candidate-invariant scene direction，而是 candidate-conditioned constraint-response rule。CNRO 以 nominal-zero signed clearance response、188D active-vs-nearest matched control、candidate-identity shuffle 验证这一命题；在没有 Full GO 前，它被明确标为 offline audit，不冒充 runtime head。

## 4. 填表

主结果表中的 `--` 应在运行完成后从 `V48.111-SUBMISSION-THREE-REGIME-SUMMARY.csv` 填写。不要从 CNRO/CATO mechanism audit 的 AUC 表复制 closed-loop 指标。
